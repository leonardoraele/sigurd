# Function: unwrapReactiveProxy()

> **unwrapReactiveProxy**\<`T`\>(`subject`): `T`

Defined in: node\_modules/@leonardoraele/signals/dist/makeReactive.d.ts:23

This function takes a reactive proxy and returns the original wrapped object. If the input is not a reactive proxy,
it simply returns the input object.

## Type Parameters

### T

`T` *extends* `object`

## Parameters

### subject

`T`

## Returns

`T`
