# API Reference

The API reference is generated locally with TypeDoc.

- Run `npm run docs:api` to regenerate the reference into `docs/public/api/`.
- Run `npm run docs` to generate the API reference and build the site together.

[Open the generated API reference](/api/index.html)
